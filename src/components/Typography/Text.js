import styled from 'styled-components';
import { color, typography, margin, display } from 'styled-system';

const Text = styled.p`
    ${color}
    ${typography}
    ${display}
    ${margin}
`;


export default Text;


