import React from 'react';
import styled from 'styled-components';

import {StyledLayer, StyledOverlay} from './StyledLayer';



export const CustomDownarrow = () => {
    return (
        <StyledLayer responsive>
            <p>DDFEl; del,dle,</p>
        </StyledLayer>);
}

export const Overlay = () => {
    return (
        <StyledOverlay >
            <p>DDFEl; del,dle,</p>
        </StyledOverlay>);
}

export default {
    title: 'Layer'
}